Usage of flooding-lib
=============================================


Usage:

    >>> import flooding_lib
    >>> import flooding_lib.admin
    >>> import flooding_lib.calc
    >>> import flooding_lib.forms
    >>> import flooding_lib.models
    >>> import flooding_lib.permission_manager
    >>> import flooding_lib.services
    >>> import flooding_lib.symbol_manager
    >>> import flooding_lib.urls
    >>> import flooding_lib.views_dev
    >>> import flooding_lib.views_infowindow
    >>> import flooding_lib.views

And we've got tools

    >>> import flooding_lib.tools.importtool.urls
    >>> import flooding_lib.tools.importtool.views


Show how to import the main classes and how to use them.
